import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  option: {
    flexBasis: '47%',
    flexGrow: 1,
    alignItems: 'center',
    gap: 8,
    borderWidth: 1,
    borderRadius: 12,
    padding: 16,
  },
});
